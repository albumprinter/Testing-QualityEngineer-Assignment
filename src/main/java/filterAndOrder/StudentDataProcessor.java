package filterAndOrder;

import filterAndOrder.models.Student;

import java.util.List;
import java.util.Map;

public class StudentDataProcessor {
    public List<Student> FilterAndOrder(){

        Map<Integer, Student> studentMap = StudentData.STUDENT_MAP;
        //Filter out the male students, and order by score (descending)
        //And return the data in a list

        return null;
    }
}
